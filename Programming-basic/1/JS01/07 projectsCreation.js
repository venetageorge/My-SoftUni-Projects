function projectsCreation(input) {
    let name = (input[0]);
    let projects = (input[1]);
    console.log(`The architect ${name} will need ${3 * projects} hours to complete ${projects} project/s.`);
}
projectsCreation(["Sanya", "9"]);